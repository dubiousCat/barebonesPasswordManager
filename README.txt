#extremely basic program that encrypts and 
decrypts strings using the same key and 
#stores encrypted strings and key in text files
#uses tkinter to make extremely basic gui

when deleting entries, type in site name 
of entry to be deleted before pressing button

delete all entries before changing key